# 从租显卡到训练出你的第一个策略 —— 全流程教程

> 目标：在一台云 GPU（RTX 4090）上，用复旦开源的 fudan_rl_wheel_leg 训练轮腿平衡步兵的 RL 策略。
> 全程约 2 小时（含训练 50 分钟），花费约几元到十几元。

## 总览

租4090 → SSH连上 → 装conda+py3.8 → 拿代码(isaacgym+复旦仓库) → 装依赖 → 冒烟测试 → 正式训练 → 导出ONNX → 下载到本地mujoco播放
→ **本地自动备份保命（第 11 步）** → **日常流程固化（第 12 步）**

> 前 8 步是「第一次跑通」；第 9~10 步是 git / 传承规范；第 11 步是**换机器、被抢占时的应对 + 自动备份**；第 12 步是**每次训练照做的日常流程**。

## 阅读约定：命令在哪台机器上跑 ⚠️

| 标记 | 在哪执行 | 提示符长相 |
|---|---|---|
| 🖥️ **本机** | 你自己的电脑（Ubuntu 桌面） | `zx@pc-ubuntu-2204:~$` |
| ☁️ **服务器** | SSH 进 GPU 实例内部 | `root@gpufree-container:~#` |
| 🔀 **混合** | 一块里两种机器都有命令 | 按块内 `# 本地` / `# 服务器` 注释分别执行 |

**分不清时敲一句 `hostname`**：显示 `pc-ubuntu-2204` 是本机，`gpufree-container` 是服务器。

最容易搞错的一处：**备份脚本 `backup_sync.sh` 永远在本机跑**（它负责把服务器上的模型拉下来），服务器上根本没有这个文件。

## 全流程一张图（日常主循环 + 被抢占分支）

### ① 日常主循环

```
🖥️ 本机：改代码 ──► git commit && git push ──► GitHub
                                                  │
              🖥️ 本机：bash ~/isaacgym/sync_to_server.sh
              （git bundle + scp；因为服务器直连 GitHub 不通）
                                                  ▼
☁️ 服务器：代码已更新 ──► 训练 train.py / 播放 play.py
                                                  │ 每 100 轮存 model_XXXX.pt
                                                  ▼
🖥️ 本机：◄── 每 5 分钟自动增量拉取 .pt + 曲线 + 日志（backup_sync.sh）
                                                  │
☁️ 服务器：导出 ONNX ──► 🖥️ 本机 scp 取回 ──► mujoco 验证
```

### ② 显卡被占用（服务器报废）

```
☁️ 旧服务器（被占用/释放，进不去也没关系）
        │  本地已有 .pt 副本 —— 自动备份的价值就在这里
        ▼
🖥️ 本机：tar 打包 server_backup 里的 run 目录 ──► scp 上传到新服务器
        ▼
☁️ 新服务器：① 按第 11.1 节部署环境（约 25 分钟）
            ② .pt 解压到 plane/logs/wheel_legged/<run名>/
            ③ --resume --load_run=<run名> --checkpoint=<步数> 继续训练（详见 11.4）
```

### ③ 服务器导出 ONNX → 本地 mujoco 验证

```bash
# ☁️ 服务器：导出
python export_onnx/export_onnx.py --load_run=<run名> --checkpoint=<步数> --out xxx.onnx
```

```bash
# 🖥️ 本机：取回并播放
scp -P <端口> root@<IP>:~/fudan_rl_wheel_leg/plane/xxx.onnx ~/isaacgym/fudan_rl_wheel_leg/mujoco/
conda activate mujoco
cd ~/isaacgym/fudan_rl_wheel_leg/mujoco
python python_tools/onnx_mj_chuanlian.py --onnx xxx.onnx
```

### 这条链上三个最容易搞错的环节 ⚠️

| 环节 | ✅ 正确做法 | ❌ 错误做法 |
|---|---|---|
| 代码 → 服务器 | `bash ~/isaacgym/sync_to_server.sh`（bundle+scp，2 秒）或服务器 `git pull`（网络通时） | 在服务器上直接改代码 → 工作区变脏，`git pull` 冲突 |
| `.pt` → 新服务器 | **scp / 网盘**（免密已配，几十秒） | 走 git（`.gitignore` 排除 + 1.2MB × N 撑爆仓库） |
| 新服务器续训 | `.pt` 放进 `logs/wheel_legged/<run名>/`，`--load_run` 与目录名一致 | 路径放错或名字不一致 → `could not find model` |

> **铁律：服务器只 pull，不改代码。** 所有改动（含学习率这类调参）都在本机提交进 git，再同步过去；`sync_to_server.sh` 会自动还原服务器上的未提交改动。

---

## 第 1 步：租显卡

**平台**：AutoDL / 算力自由 / 优云智算 等，选 4090 即可。

**配置要点**：
| 选项 | 选什么 | 为什么 |
|---|---|---|
| GPU | RTX 4090（24G 独享） | 24G 显存跑 4096 个并行环境 |
| 实例类型 | 独占式 | 抢占式训练中途可能被回收 |
| 系统 | Ubuntu 22.04 | Isaac Gym 官方支持 |
| 镜像 | **系统镜像 Ubuntu-nvidia（虚机）** | 干净 + 自带驱动；**别选 PyTorch 镜像**（py3.12 用不上，Isaac Gym 锁死 py3.8） |
| 计费 | 按量计费 | 关机不收费，用完就关 |

开机后平台会给你：**IP、端口、用户名（root 或 ubuntu）、密码**。

---

## 第 2 步：SSH 连接 + 免密

> 🖥️ **本机** —— 在你自己电脑的终端里执行

```bash
# 密码登录（端口用平台的）
ssh -p <端口> root@<IP>          # 输密码（注意特殊字符，建议复制粘贴）

# 配置免密（一劳永逸）
ssh-keygen -t ed25519            # 一路回车
ssh-copy-id -p <端口> root@<IP>  # 输一次密码
ssh -p <端口> root@<IP>          # 以后免密
```

---

## 第 3 步：装 Miniconda + 建 leg 环境

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
# 服务器上执行
cd /tmp
wget https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash Miniconda3-latest-Linux-x86_64.sh -b -p $HOME/miniconda3
export PATH=$HOME/miniconda3/bin:$PATH
source $HOME/miniconda3/etc/profile.d/conda.sh

# ⚠️ 坑：新版 conda 有"条款门禁"，不先接受会建环境失败
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r

# 建环境：Isaac Gym 只支持 Python 3.8！
conda create -n leg python=3.8 -y -c https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main --override-channels
conda activate leg
```

---

## 第 4 步：拿代码

**isaacgym 本体**（NVIDIA 官网需注册账号，从你电脑打包上传最省事）：
> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 本地电脑
tar -cf isaacgym_python.tar ~/isaacgym/python      # ~438MB
scp -P <端口> isaacgym_python.tar root@<IP>:~/     # 或用平台网页上传

# 服务器
mkdir -p ~/isaacgym && tar -xf isaacgym_python.tar -C ~/isaacgym
```

**复旦仓库**（git clone 可能慢/中断，打包上传更稳）：
> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 方式 A：服务器上直接 clone（网络好时）
git clone https://github.com/yly-true/fudan_rl_wheel_leg.git

# 方式 B：本地打包上传（推荐，170MB）
tar -cf fudan_repo.tar ~/isaacgym/fudan_rl_wheel_leg
scp -P <端口> fudan_repo.tar root@<IP>:~/
# 服务器: rm -rf fudan_rl_wheel_leg && tar -xf fudan_repo.tar
```

---

## 第 5 步：安装依赖

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
conda activate leg
export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH

# 1) Isaac Gym（清华 pypi 源加速，会下载 torch 2.4.1 约 2.5GB，几分钟）
cd ~/isaacgym/python
pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple

# 2) ⚠️ 坑：numpy 1.24 删除了 np.float，Isaac Gym 老代码会报错，打补丁
sed -i 's/dtype=np.float,/dtype=float,/' ~/isaacgym/python/isaacgym/torch_utils.py

# 3) 复旦仓库
cd ~/fudan_rl_wheel_leg/plane
pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple

# 4) 训练/导出依赖
pip install tensorboard gitpython matplotlib onnx -i https://pypi.tuna.tsinghua.edu.cn/simple
```

---

## 第 6 步：验证（冒烟测试）

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
# 导入验证
python -c "
import isaacgym
from wheel_legged_gym.envs import *
from wheel_legged_gym.utils import task_registry
print('OK', list(task_registry.task_classes.keys()))
"   # 首次会编译 gymtorch（1-3分钟）

# 短训 2-3 轮验证 GPU 链路
cd ~/fudan_rl_wheel_leg/plane
python wheel_legged_gym/scripts/train.py --task=wheel_legged --headless --num_envs=1024 --max_iterations=3
# 看到 "Physics Device: cuda:0" + Mean reward 输出 = 通过
```

---

## 第 7 步：正式训练

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
cd ~/fudan_rl_wheel_leg/plane
nohup python wheel_legged_gym/scripts/train.py --task=wheel_legged --headless \
      --num_envs=4096 --max_iterations=5000 > train.log 2>&1 &
# nohup + & = 关掉 SSH 也不中断

# 实时看进度
tail -f train.log
# 看曲线（服务器开 8080 端口，本地 SSH 隧道访问）
tensorboard --logdir logs --port 8080
# 本地: ssh -p <端口> -L 8080:localhost:8080 root@<IP> 然后浏览器 http://localhost:8080
```

**⚠️ 坑（我们踩过）**：默认学习率 1e-3 训练会发散（奖励卡 -190 然后 NaN 崩溃）。
改配置：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
sed -i 's/learning_rate = 1.0e-3  # 5.e-4/learning_rate = 5.0e-4/' \
  wheel_legged_gym/envs/base/legged_robot_config.py
```

**怎么选 checkpoint**：训练中奖励最高的阶段才是有用的模型，最终模型可能"后期崩塌"。
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
# 看奖励趋势
awk '/Total timesteps/{ts=$NF} /Mean reward/{print ts, $NF}' train.log | tail -30
# checkpoint 存在: logs/wheel_legged/<run名>/model_XXXX.pt （每100轮存一个）
```

---

## 第 8 步：导出 ONNX + 下载到本地

> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 服务器：导出 ONNX
cd ~/fudan_rl_wheel_leg/plane
python export_onnx/export_onnx.py --load_run=<run名> --checkpoint=<步数> --out my_policy.onnx

# 下载回本地（下面这条在【本机】执行）
# 本机: scp -P <端口> root@<IP>:~/my_policy.onnx ~/isaacgym/fudan_rl_wheel_leg/mujoco/
```

**本地 mujoco 播放自己的策略**：
> 🖥️ **本机** —— 在你自己电脑的终端里执行

```bash
conda activate mujoco
cd ~/isaacgym/fudan_rl_wheel_leg/mujoco
python python_tools/onnx_mj_chuanlian.py --onnx my_policy.onnx
# b 开力矩 → 1 前进 → 空格跳跃 → Esc 退出
```

---

## 坑速查表

| 症状 | 原因 | 解决 |
|---|---|---|
| libpython3.8.so.1.0 找不到 | LD_LIBRARY_PATH 没设 | `export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:$LD_LIBRARY_PATH` |
| numpy no attribute 'float' | numpy 1.24 删别名 | sed 补丁 isaacgym/torch_utils.py |
| conda 建环境失败(条款) | conda 26 TOS 门禁 | conda tos accept |
| Could not find env leg | 建环境其实失败了 | 检查 conda env list，重跑 create |
| 训练 reward 卡死负值/NaN崩溃 | 学习率太高 | 降到 5e-4 或更低 |
| 训练后期崩塌(reward 回落) | RL 常见，策略不稳 | 用中期 checkpoint；调参阶段解决 |
| gymtorch 编译报 Ninja | PATH 没有 env/bin | `export PATH=$CONDA_PREFIX/bin:$PATH` |
| onnx 导出报 Module onnx not installed | 缺包 | pip install onnx |
| git clone 只有几 KB/s、卡住 | 国内直连 GitHub 慢 | 本地打包 scp 上传（第 11 步③） |
| git 报 dubious ownership | tar 保留了原机属主 | `chown -R root:root <repo>` |
| 服务器没有 rsync / apt update 卡死 | 镜像精简 + 容器 IPv6 挂起 | `apt-get -o Acquire::ForceIPv4=true install -y rsync` |
| 换机器后 conda activate leg 找不到环境 | 没做 conda init | `/root/miniconda3/bin/conda init bash` 后重登（第 11 步①） |


---

## 第 9 步（进阶）：git 版本管理 —— 留日志、可传承、双机同步

> 目的：本地改代码 → 推送 GitHub → 服务器 git pull 拉取。不用每次重传 170MB。
> 仓库示例：https://github.com/zhangxxuu/RL_wheel_leg（fork 自复旦，含部署工具）

### 9.1 一次性的初始化（本地）

> 🖥️ **本机** —— 在你自己电脑的终端里执行

```bash
# ① 配置身份（第一次用 git 时）
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"

# ② 添加 SSH 密钥到 GitHub（Settings → SSH and GPG keys）
#    本地生成（没有的话）: ssh-keygen -t ed25519
#    复制公钥: cat ~/.ssh/id_ed25519.pub
#    测试: ssh -T git@github.com → Hi xxx! successfully authenticated

# ③ remote 指向你的仓库
cd ~/isaacgym/fudan_rl_wheel_leg
git remote set-url origin git@github.com:<你>/<仓库>.git
git remote add upstream https://github.com/yly-true/fudan_rl_wheel_leg.git  # 留复旦原版
```

### 9.2 日常循环（改代码 → 上服务器）

> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 本地：改完提交推送
cd ~/isaacgym/fudan_rl_wheel_leg
git add .
git commit -m "改了什么：orientation 惩罚 -100→-150，目标高度 0.18→0.20"
git push

# 服务器：拉取（等于同步最新代码）
cd ~/fudan_rl_wheel_leg && git pull
# ⚠️ 服务器连不上 GitHub 时（实测常见）→ 在【本机】执行 bash ~/isaacgym/sync_to_server.sh
```

### 9.3 拉复旦的最新更新（偶尔）

> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 本机或服务器都行（哪个仓库要更新就在哪跑）
git pull upstream main     # 复旦更新了会合并进来（可能冲突，需手动解决）
```

### 9.4 新租一台服务器时的快速部署（有 git 之后）

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
# 装好 miniconda + leg 环境后，直接 clone 你的仓库（不用打包上传！）
git clone git@github.com:<你>/<仓库>.git ~/fudan_rl_wheel_leg
cd ~/fudan_rl_wheel_leg/plane && pip install -e .
# isaacgym 本体仍需传一次（438MB，不在 git 里）
```

### 9.5 传承好习惯

| 习惯 | 说明 |
|---|---|
| commit message 写清楚 | "改了什么 + 为什么"，几个月后 git log 就是调参史 |
| 模型不提交 | .pt/.onnx 用 run 名+checkpoint 号记录（可放 release/网盘） |
| 训练记录留档 | 每次训练的参数/效果写进 message 或单独 notes 文件 |
| 跟着复旦学命名 | 他们的 run 名就是调参笔记："上台阶3_angz+添加随机地形" |

### 常见问题

| 症状 | 原因 | 解决 |
|---|---|---|
| push 被拒 (non-fast-forward) | 远程有新提交 | `git pull` 合并后再 push |
| Permission denied (publickey) | SSH 密钥没加对 | 重加完整公钥到 GitHub；`ssh -T git@github.com` 测试 |
| 合并冲突 | 两边改了同一处 | 手动编辑冲突文件 → `git add` → `git commit` |



---

## 第 10 步：项目传承与规范 —— 模型不进 git，传承靠"记录"

> 为什么仓库里没有 .pt？为什么复旦的 run 名长得像调参笔记？——因为 **git 管代码，模型靠"run 名 + checkpoint 号 + 记录卡"传承**。这是战队级规范，让半年后的你和队友能复现每一次实验。

### 10.1 为什么 git 里没有 .pt 模型？

| 类型 | 进 git? | 理由 | 怎么处理 |
|---|---|---|---|
| 代码 / 配置 | ✅ | 版本管理核心，可 diff | 正常提交 |
| 模型权重 .pt | ❌ | 几十~几百 MB；二进制无 diff 价值；每 100 轮存一个，训 5 万轮 = 500 个，仓库必爆 | 留在服务器 `logs/`，用 **run 名 + checkpoint 号**记录；分享走网盘 / GitHub Release |
| ONNX 成品 | ✅ | 只含推理权重（157KB 级），适合分享部署 | 常用成品进 git（放 `mujoco/` 目录），试验品用名字记录 |
| run 目录配置快照(.py) | ✅ | 自动保存的当时参数，是调参宝藏 | 训练自动生成，直接提交 |

`.gitignore` 已帮你写好（`model_*.pt` / `*.pth` 等全忽略），push 不会误传模型——**别用 `git add -f` 强推**。

### 10.2 run 目录里的小 .py 是什么？（配置快照）

每次训练启动时，legged_gym 会把当时的配置复制一份进 run 目录（save_cfgs 干的）：

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```
logs/wheel_legged/Sep04_10-01-32_/
├── legged_robot_config.py   ← 当时的学习率 / PD / 奖励权重
├── legged_robot.py          ← 当时的环境代码
├── model_400.pt ...         ← 模型（被 gitignore 掉了，不进 git）
```

`git log` 只告诉你代码改过什么；**这份快照告诉你"这个模型当时用什么参数训出来的"**——两者一起才能复现实验。复旦的日志里只能看到这些 .py，因为 .pt 全留在他们服务器上，分享成品只发 ONNX。

### 10.3 用名字定位一个模型（task / experiment_name / load_run / checkpoint）

| 参数 | 作用 | 决定什么 |
|---|---|---|
| `--task=wheel_legged` | 从注册表选任务（机器人 / URDF / 奖励） | 跑哪个任务 |
| `--experiment_name=wheel_legged` | 日志目录叫什么 | `logs/wheel_legged/` |
| `--load_run=Sep04_10-01-32_` | 从哪个 run 目录加载 | 那次训练的所有 checkpoint |
| `--checkpoint=600` | 加载第几轮的模型 | `model_600.pt` |

**加载路径 = `logs/<experiment_name>/<load_run>/model_<checkpoint>.pt`**
`task` 决定"跑什么"，`experiment_name` 决定"记到哪个文件夹"——两者可以不一样（`--task=wheel_legged --experiment_name=我的实验A`），同名只是复旦作者懒得起名 😄

**命令速查**（复旦脚本.txt 全解，`--load_run` 换成你的 run 名）：

| 想干什么 | 命令 |
|---|---|
| 从头训练 | `python wheel_legged_gym/scripts/train.py --task=wheel_legged --headless`（默认 8192 环境 / 5 万轮） |
| 从头训练·控资源 | 上面加 `--num_envs=4096 --max_iterations=5000` |
| 续训（重点） | 上面加 `--resume --experiment_name=wheel_legged --load_run=<run名> --checkpoint=<步数>` |
| 播放指定模型 | `python wheel_legged_gym/scripts/play.py --task=wheel_legged --load_run=<run名> --checkpoint=<步数>` |
| 导出 ONNX | `python export_onnx/export_onnx.py --load_run=<run名> --checkpoint=<步数> --out xxx.onnx` |
| 不写 load_run | train/play 自动用**最新 run** 的最新 checkpoint（play.py 默认行为） |
| 训练用 10 帧历史 | 导出要改用 `export_onnx_his10.py`（默认训练是 5 帧，用 export_onnx.py） |

### 10.4 传承规范（写给队友和你自己）

1. **commit message 模板**：
   ```text
   feat(train): 上台阶策略 奖励base_height=1.5
   - 迭代5000轮, num_envs=4096, lr=5e-4
   - 效果: mujoco能上台阶; 待实车验证
   ```
2. **模型留档**：训出好模型 → 导出ONNX + 记录 run名/步数/配置 → ONNX 放 git 或网盘，写清效果
3. **每台服务器部署完更新"自查清单"**（附录里那份），新人照表走
4. **log run 名要有意义**：像复旦那样 `上台阶3_angz+随机地形`，别用默认时间戳跑正式实验
5. **README 维护**：把每次成功的"任务-配置-效果"记进仓库 README 或 EXPERIMENTS.md



---

## 第 11 步：换机器不慌 —— 新机部署 + 本地自动备份 + 被占用应对

> 场景：按量计费的实例关机后 GPU 被别的用户占满，只能重租一台。**新机器是空白的（连 conda 都没有）**，所以要把第 3~6 步再走一遍——本步是"第三次部署"的实战总结，含 4 个新坑和一套保命机制。

### 11.1 新机器部署（实测全程约 25 分钟）

**① 装 Miniconda + leg 环境（约 3 分钟）**

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
curl -sL -o /root/miniconda.sh https://mirrors.tuna.tsinghua.edu.cn/anaconda/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash /root/miniconda.sh -b -p /root/miniconda3
/root/miniconda3/bin/conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
/root/miniconda3/bin/conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r
/root/miniconda3/bin/conda create -n leg python=3.8 -y \
    -c https://mirrors.tuna.tsinghua.edu.cn/anaconda/pkgs/main --override-channels
/root/miniconda3/bin/conda env config vars set -n leg LD_LIBRARY_PATH=/root/miniconda3/envs/leg/lib
/root/miniconda3/envs/leg/bin/pip config set global.index-url https://pypi.tuna.tsinghua.edu.cn/simple
/root/miniconda3/bin/conda init bash        # 以后登录直接 conda activate leg
```

**② 传 isaacgym（本地 437MB，上传约 2 分钟）**

> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 本地（在 ~/isaacgym 里打包，包内路径就是 python/ 开头）
cd ~/isaacgym && tar -cf isaacgym_python.tar python
scp -P <端口> isaacgym_python.tar root@<IP>:/root/

# 服务器
mkdir -p /root/isaacgym && tar -xf /root/isaacgym_python.tar -C /root/isaacgym   # → /root/isaacgym/python
```

**③ 传代码仓库：不要用 git clone（实测数据）**

| 方式 | 速度 | 170MB 耗时 |
|---|---|---|
| `git clone` 直连 GitHub | ~25 KB/s | **≈2 小时（基本卡死）** |
| 本地打包 + scp 上传 | ~3~5 MB/s | **≈40 秒** ⭐ |

> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 本地：连 .git 一起打包（保留提交历史与 remote，以后服务器还能 git pull）
cd ~/isaacgym && tar -cf fudan_repo.tar fudan_rl_wheel_leg
scp -P <端口> fudan_repo.tar root@<IP>:/root/

# 服务器
cd /root && tar -xf fudan_repo.tar
chown -R root:root /root/fudan_rl_wheel_leg      # 不 chown 会报 git "dubious ownership"
```

**④ 装依赖（约 5 分钟，torch 797MB 走清华源）**

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
cd /root/isaacgym/python
/root/miniconda3/envs/leg/bin/pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple
sed -i 's/dtype=np.float,/dtype=float,/' /root/isaacgym/python/isaacgym/torch_utils.py    # np.float 补丁

cd /root/fudan_rl_wheel_leg/plane
/root/miniconda3/envs/leg/bin/pip install -e . -i https://pypi.tuna.tsinghua.edu.cn/simple
/root/miniconda3/envs/leg/bin/pip install tensorboard gitpython matplotlib onnx \
    -i https://pypi.tuna.tsinghua.edu.cn/simple
```

**⑤ 学习率补丁（防发散）**

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
sed -i 's/learning_rate = 1.0e-3  # 5.e-4/learning_rate = 5.0e-4/' \
  /root/fudan_rl_wheel_leg/plane/wheel_legged_gym/envs/base/legged_robot_config.py
```

**⑥ 验证（两步，缺一不可）**

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
# 导入验证（首次会编译 gymtorch，1~3 分钟）
cd /root && python -c "
import isaacgym
from wheel_legged_gym.envs import *
from wheel_legged_gym.utils import task_registry
print('FULL_IMPORT_OK', list(task_registry.task_classes.keys()))"
# 期望：FULL_IMPORT_OK ['wheel_legged']

# GPU 冒烟测试（3 轮）
cd /root/fudan_rl_wheel_leg/plane
python wheel_legged_gym/scripts/train.py --task=wheel_legged --headless --num_envs=1024 --max_iterations=3
# 期望：Physics Device: cuda:0 + Mean reward 滚动
```

**⑦ 本次新踩的 4 个坑**

| 症状 | 原因 | 解决 |
|---|---|---|
| `git clone` 只有几 KB/s | 国内直连 GitHub 太慢 | **本地打包 scp 上传**，别硬等 |
| `fatal: detected dubious ownership` | tar 保留了原机属主（zx），服务器是 root | `chown -R root:root <repo>` |
| `RuntimeError: Ninja is required to load C++ extensions` | 没走 `conda activate`，`env/bin`（含 ninja 可执行文件）不在 PATH | 用 `conda activate leg`；非交互调用时 `export PATH=/root/miniconda3/envs/leg/bin:$PATH` |
| 服务器没有 rsync / `apt-get update` 卡死 | 镜像精简没装 + 容器里 IPv6 挂起 | `apt-get -o Acquire::ForceIPv4=true install -y rsync`（先 `kill` 掉卡住的 apt 进程并删锁） |

### 11.2 GPU 被占用 / 实例关机了怎么办

**平台给的三条路**（AutoDL / 算力自由 这类按量平台）：

| 选项 | 花费 | 能干什么 |
|---|---|---|
| **无卡模式开机** ⭐ | ~0.1 元/小时 | CPU 模式进去搬数据、push 代码、整理日志；**不能训练**（`nvidia-smi` 报错、isaacgym 起不来） |
| **克隆实例** | 新实例按量计费 | 平台把镜像复制到有空卡的主机 → 环境/数据全在，直接续训 |
| **等待释放** | 0 | 数据保留，时间不确定 |
| 包年包月（独占） | 约 1500~2500 元/月 | 独享 GPU；**单次 1 小时的实验不值得**，被抢占最多损失 1~2 分钟训练量 |

**克隆报「源实例未调度成功」**：意思是源实例所在主机排不到 GPU，做不出镜像 → 这条路暂时走不通，**别在克隆上耗时间**，用无卡模式抢救数据。

**无卡模式抢救数据**

> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 平台面板对【旧实例】点"无卡模式开机"，用新端口 SSH 进去
cd ~/fudan_rl_wheel_leg/plane
tar -czf ~/old_backup.tar.gz logs/wheel_legged/<run名>/ train*.log
# 本地拉回
scp -P <新端口> root@<IP>:~/old_backup.tar.gz ~/isaacgym/server_backup/
```

⚠️ **关机 ≠ 释放**：关机数据保留，**点「释放实例」等于删盘**，没确认备份齐之前千万别点。

> 真正省心的解法是下面 11.3 的自动备份：**数据永远有本地副本，就不用跟平台斗智斗勇。**

### 11.3 本地自动备份（保命配置）⭐

**原理**：在**你自己的电脑**上跑一个循环，每 5 分钟把服务器上的 `.pt` / 日志 / tensorboard 曲线**增量**拉到本地。服务器被抢占、被释放，模型都还在手里。

**为什么必须跑在本地**：拉取端必须跟数据落地端在同一台机器上（服务器没法反向连你的电脑）；而且 AI 代理帮不了你——它的命令跑在沙箱里（`bwrap --die-with-parent`），起的后台进程必被回收，**只能你自己在终端启动**。

**一次性配置（3 步）**

> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# ① 本机 → 服务器 免密（之后才能自动拉）
ssh-copy-id -p <端口> -i ~/.ssh/id_ed25519.pub root@<IP>

# ② 服务器装 rsync（平台镜像里常常没有）
ssh -p <端口> root@<IP> 'apt-get -o Acquire::ForceIPv4=true -y install rsync'

# ③ 本机启动常驻循环
bash ~/isaacgym/backup_sync.sh start
bash ~/isaacgym/backup_sync.sh status
```

**用到的文件**（都在 `~/isaacgym/`，脚本也随仓库提供：`tools/backup_sync.sh`）

| 文件 | 作用 |
|---|---|
| `backup_sync.sh` | 同步主程序：`once` / `start` / `stop` / `status` |
| `backup_sync.conf` | 服务器 IP + 候选端口列表 + 远端目录 |
| `backup_sync.log` | 运行日志 |
| `server_backup/plane/` | 备份落地目录（保持服务器上的目录结构） |

- **拉什么**：`*.pt`（模型）+ `*.py`（run 目录里的配置快照）+ `train*.log` + `events.out.tfevents*`（曲线）
- **体积参考**：每个 `.pt` 只有 **~1.2MB**，训满 5 万轮（500 个 checkpoint）也才 ~600MB，本地随便存
- **增量很快**：首次约 3 分钟（建目录树），之后每次 **3 秒左右**
- **换端口/换机器**：改 `backup_sync.conf` 里的 `PORTS`（可写多个候选，脚本自动挑能连上的）→ **`stop` 再 `start`**（运行中的循环只在启动时读一次配置）
- **开机自启（可选）**：`crontab -e` 加一行

> 🖥️ **本机** —— 在你自己电脑的终端里执行

```
*/5 * * * * /bin/bash /home/zx/isaacgym/backup_sync.sh once >> /home/zx/isaacgym/backup_sync.log 2>&1
```

- **电脑关机/休眠期间不拉**，醒来后下一轮自动补齐增量（不会漏文件）

**服务器彻底没了也别慌**：本地 `.pt` 可以纯 CPU 导出 ONNX（只用 torch + onnx，不碰 isaacgym、不需要 GPU）

> 🖥️ **本机** —— 在你自己电脑的终端里执行

```bash
cd ~/isaacgym/fudan_rl_wheel_leg/plane
python export_onnx/export_onnx.py \
  --log_root ~/isaacgym/server_backup/plane/logs/wheel_legged \
  --load_run=<run名> --checkpoint=<整百数> --out my.onnx

# 再本地 mujoco 播放（流畅，比远程桌面强）
conda activate mujoco && cd ../mujoco
python python_tools/onnx_mj_chuanlian.py --onnx my.onnx
```

### 11.4 服务器没了：用本地 .pt 在新机器上"复活"训练 ⭐

**场景**：服务器被抢占/释放，但本地自动备份里有 `.pt`（`~/isaacgym/server_backup/plane/logs/wheel_legged/<run名>/`）。怎么让新服务器接着训？

**原理**：续训靠 `--resume --load_run=<run名> --checkpoint=<步数>`，它按固定路径找文件：

```
logs/<experiment_name>/<load_run>/model_<checkpoint>.pt
```

所以**只要把本地那份放到这个位置**，就能无缝续训（`.pt` 里存着策略权重 + 优化器状态 + 迭代计数，不是重新开始）。

**完整流程（4 步）**

```bash
# 🖥️ 本机：打包本地备份里的 run 目录（想选哪轮就带哪轮）
cd ~/isaacgym/server_backup/plane/logs/wheel_legged
tar -czf ~/resume_run.tar.gz Sep11_00-00-39_

# 🖥️ 本机：传到新服务器（免密已配，秒级）
scp -P <新端口> ~/resume_run.tar.gz root@<新IP>:/root/
```

```bash
# ☁️ 新服务器：解压到标准位置（新机器要先按第 11.1 节部署好环境）
cd /root/fudan_rl_wheel_leg/plane/logs/wheel_legged
tar -xzf /root/resume_run.tar.gz
ls Sep11_00-00-39_/model_*.pt          # 确认 checkpoint 到位

# ☁️ 新服务器：接着训（checkpoint 号 = 文件名里的数字）
cd /root/fudan_rl_wheel_leg/plane
nohup python wheel_legged_gym/scripts/train.py --task=wheel_legged --headless \
    --resume --experiment_name=wheel_legged \
    --load_run=Sep11_00-00-39_ --checkpoint=500 \
    --num_envs=4096 --max_iterations=3000 > train2.log 2>&1 &
tail -f train2.log
# 看到 Loading model from: .../model_500.pt 就是接上了
```

**只想看效果、不训练？** 连服务器都不用：本地纯 CPU 导出 ONNX（第 11.3 节末尾）+ mujoco 播放。

**几个必须踩准的点**

| 点 | 说明 |
|---|---|
| 路径必须对 | 必须放在 `plane/logs/wheel_legged/<run名>/` 下，run 名可自定义，但 `--load_run` 要和目录名一致 |
| checkpoint 号 | `model_500.pt` → `--checkpoint=500`；不写 `--checkpoint` 默认取最新那个 |
| 每 100 轮存一个 | `save_interval = 100`，所以最坏丢 99 轮（约 1 分钟训练量） |
| **别用 git 传 .pt** | `.gitignore` 明确排除 `**/logs/**/model_*.pt`；每个 1.2MB，仓库会被撑爆 → 用 scp / 网盘 / GitHub Release |
| 配置快照(.py)可选 | `.pt` 是必需的；续训时会自动在新 run 目录写入当前配置快照 |
| 代码版本要一致 | 新旧服务器用同一版代码，否则网络结构不匹配、`load_state_dict` 报错（用 git bundle / scp 同步代码） |
| 别覆盖正在写的 .pt | 目标机上如果还有同名 run 在训练，先停训练再传 |

**这一套 + 自动备份 = 完整保险**：本机每 5 分钟落一份模型 → 服务器被抢占 → 新机器 25 分钟部署 + 40 秒传 `.pt` → 直接续训，不损失进度。

---

## 第 12 步：日常流程（每次训练照这个走）

### 12.1 开机后的 5 分钟

1. 平台开机 → 记下 **IP + 端口**（重启后可能变）
2. 端口变了 → 更新 `~/isaacgym/backup_sync.conf` → `bash ~/isaacgym/backup_sync.sh stop && bash ~/isaacgym/backup_sync.sh start`
3. `ssh -p <端口> root@<IP>` → `conda activate leg`
4. `cd ~/fudan_rl_wheel_leg && git pull`（把本地最新代码/文档同步上去）
5. `nvidia-smi` 确认卡是空的（别人没用）
6. **新机器或动过环境** → 做一次 3 轮冒烟测试（第 11 步⑥）

### 12.2 开训

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
cd ~/fudan_rl_wheel_leg/plane
nohup python wheel_legged_gym/scripts/train.py --task=wheel_legged --headless \
    --num_envs=4096 --max_iterations=6000 > train.log 2>&1 &
tail -f train.log
```

- **续训**（接着上次的 checkpoint）：上面命令再加
  `--resume --experiment_name=wheel_legged --load_run=<run名> --checkpoint=<步数>`
- 看到 `Physics Device: cuda:0` + `Mean reward` 开始滚动 = 正常
- `--num_envs` 默认 8192（吃满显存），4096 更稳；`--max_iterations` 默认 5000（本任务配置里是 50000）

### 12.3 训练中（每 15~30 分钟瞄一眼）

| 想看什么 | 怎么看 |
|---|---|
| 进度 / 速度 / ETA | `tail -5 train.log` |
| 奖励曲线 | 服务器 `tensorboard --logdir logs --port 8080` + 本地 `ssh -p <端口> -L 8080:localhost:8080 root@<IP>` → 浏览器 `http://localhost:8080` |
| 是否发散 | reward 卡在负值 / 变 NaN → 学习率降到 5e-4 或更低（第 7 步的坑） |
| 机器人现在什么样 | 导出当前 checkpoint 的 ONNX → **本地 mujoco 播放**（比远程桌面流畅得多） |
| 模型是否安全落地 | 本机 `bash ~/isaacgym/backup_sync.sh status` |

**关键指标速读**

| 指标 | 好的表现 | 含义 |
|---|---|---|
| `Mean length` | 满格（2002） | 整回合不摔（摔了会提前终止） |
| `Mean reward` | 稳定为正、缓慢上升 | 策略在进步；长期不动或下滑要查原因 |
| `rew_tracking_lin_vel` | 越大越好 | 速度指令跟踪得好不好 |
| `rew_base_height` | 正数 | 机身高度贴在目标值附近（站起来的关键） |
| `rew_torques` / `rew_dof_acc` | 越接近 0 越好 | 力矩/加速度惩罚，动作是否粗暴 |

### 12.4 停训 + 收尾

> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
# 停：先看 PID 再精准杀（别用 pkill -f，容易把自己的 shell 也杀了）
ps aux | grep [t]rain.py
kill <PID>
```

1. **挑最佳 checkpoint**：不一定是最后一个——看曲线选奖励最高的阶段（RL 后期可能"崩塌"）
2. **导出 ONNX**：`python export_onnx/export_onnx.py --load_run=<run名> --checkpoint=<步数> --out xxx.onnx`
3. **拉回本地**：`scp -P <端口> root@<IP>:~/fudan_rl_wheel_leg/plane/xxx.onnx ~/isaacgym/fudan_rl_wheel_leg/mujoco/`（`.pt` 一般已被自动备份拉到本地）
4. **mujoco 验证**：`python python_tools/onnx_mj_chuanlian.py --onnx xxx.onnx`
5. **写记录卡 + 提交**（第 10 步模板）：
   `git commit -m "feat(train): 上台阶策略 base_height=1.5；run Sep10_xx，ckpt 3000，mujoco 验证可上台阶"`
6. **不用了就关机**（别急着"释放"；要释放先确认本地备份齐了）

### 12.5 常用命令速查

| 目的 | 命令 |
|---|---|
| 连服务器 | `ssh -p <端口> root@<IP>` |
| 激活环境 | `conda activate leg` |
| 看 GPU 占用 | `nvidia-smi` |
| 从头训练 | `python wheel_legged_gym/scripts/train.py --task=wheel_legged --headless --num_envs=4096` |
| 续训 | 上面 + `--resume --experiment_name=wheel_legged --load_run=<run> --checkpoint=<步数>` |
| 播放（需远程桌面 + pynput） | `python wheel_legged_gym/scripts/play.py --task=wheel_legged --load_run=<run> --checkpoint=<步数>` |
| 导出 ONNX | `python export_onnx/export_onnx.py --load_run=<run> --checkpoint=<步数> --out x.onnx` |
| 停训练 | `ps aux \| grep [t]rain.py` → `kill <PID>` |
| 看备份状态（**本机**） | `bash ~/isaacgym/backup_sync.sh status` |
| 代码同步（**本机**） | `bash ~/isaacgym/sync_to_server.sh`（bundle+scp，服务器连不上 GitHub 时用这个） |
| 代码同步（备用） | 网络通时：服务器 `cd ~/fudan_rl_wheel_leg && git pull` |
| 同步复旦最新 | 服务器/本地 `git pull upstream main` |

### 12.6 养成这几个习惯

1. **run 名写清参数意图**（"上台阶3_angz+随机地形"），别用默认时间戳跑正式实验
2. **一次只改 1~2 个变量**，否则出问题不知道是谁的锅
3. **单次训练控制在 1 小时内**：迭代快、被抢占损失小、容易对比
4. **每训完一轮就留记录卡**（第 10 步模板）+ 导出 ONNX 存好
5. **每天结束**：确认本地备份已同步（`status`）+ `git push` 当天改动

---

## 附录：第二次部署踩坑记录（2026-09）

### 坑 1：pip 装错环境（Python 3.14 vs 3.8）

**症状**：提示符是 `(leg)`，但 pip 装到 base（路径含 `lib/python3.14/site-packages`），isaacgym 报 `requires a different Python: 3.14.7`。

**原因**：终端 PATH 混乱，`pip` 指向了 base 的 python。

**解决（最保险）**：永远用**绝对路径**，不依赖环境激活：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
/root/miniconda3/envs/leg/bin/pip install -e /root/isaacgym/python -i https://pypi.tuna.tsinghua.edu.cn/simple
/root/miniconda3/envs/leg/bin/python -c "import torch; print(torch.__version__)"   # 验证也用绝对路径
```

**验证环境对不对**：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
conda activate leg
which python    # 必须是 /root/miniconda3/envs/leg/bin/python
python --version  # 必须是 3.8.x
```

### 坑 2：git clone 卡在 89%

**原因**：仓库含完整历史（170MB），服务器从 GitHub 下载大包慢。

**解决**：浅克隆（服务器只要最新代码，历史留本地）：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
git clone --depth 1 https://github.com/zhangxxuu/RL_wheel_leg.git fudan_rl_wheel_leg
```

### 坑 3：git push 被拒 non-fast-forward

**原因**：GitHub 远程有本地没有的提交（复旦更新了 / 建仓库时自动生成了提交）。

**解决**：先合并再推：
> 🔀 **混合** —— 同一块里「本机」和「服务器」的命令都有，按块内注释分别执行

```bash
# 在哪个仓库遇到就在哪执行（本机/服务器都可能是）
git pull origin main --no-rebase   # 合并远程新提交
git push origin main
```

### 坑 4：import isaacgym "假成功"

**原因**：在 `/root/isaacgym/python` 目录里跑 `python -c "import isaacgym"`——python 把当前目录加入搜索路径，碰巧能导入，**不代表装好了**。

**解决**：验证时先 `cd ~` 再测；或看 `pip show isaacgym`。



### 坑 5：NGC 容器自带系统 conda（/opt/conda），激活找不到 leg

**症状**：`conda activate leg` 报 `EnvironmentNameNotFound`，但 `ls /root/miniconda3/envs/leg` 明明存在。

**原因**：算力平台镜像（NGC 容器）自带一个系统 conda（`/opt/conda`），shell 用的可能是它；leg 装在你自己的 `/root/miniconda3` 里，两个 conda 互不认识。

**解决**：把**你自己的** conda 初始化进 .bashrc（一次性）：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
/root/miniconda3/bin/conda init bash
# 然后重新登录 SSH（或开新终端）
conda activate leg    # 现在正常了
```
或者不重登也行，手动指路：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
export PATH=/root/miniconda3/bin:$PATH
source /root/miniconda3/etc/profile.d/conda.sh
conda activate leg
```

### 坑 6：pip 大安装（torch 2.5GB）总被中断

**症状**：`nohup pip install ... &` 后台跑一会就没了，日志空的。

**原因**：SSH 会话关闭 / 终端控制把后台进程杀了（有的平台容器会回收）。

**解决（二选一）**：
1. **前台装，耐心等**（推荐，最稳）——torch 走缓存后其实挺快：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
/root/miniconda3/envs/leg/bin/pip install -e /root/isaacgym/python -i https://pypi.tuna.tsinghua.edu.cn/simple
```
2. 或服务器端 setsid 完全脱离（SSH 断开不影响）：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
setsid bash -c '/root/miniconda3/envs/leg/bin/pip install -e /root/isaacgym/python -i https://pypi.tuna.tsinghua.edu.cn/simple > /root/pip.log 2>&1' < /dev/null &
```

### 坑 7：装完后 import 又报 libpython3.8.so.1.0

**原因**：LD_LIBRARY_PATH 没设置（每次新会话都会忘）。

**解决**：固化到 leg 环境（一次性，以后 activate 自动带上）：
> ☁️ **服务器** —— SSH 进 GPU 实例后执行

```bash
conda activate leg
conda env config vars set LD_LIBRARY_PATH=$CONDA_PREFIX/lib
conda deactivate && conda activate leg
```

---

## 📋 服务器部署最终自查清单（每次新机器照这个过）

| # | 检查 | 命令 |
|---|---|---|
| 1 | conda 是哪个 | `which conda` → 应是 `/root/miniconda3/bin/conda` |
| 2 | leg 能激活 | `conda activate leg` + `which python` → envs/leg/bin/python 3.8 |
| 3 | 包装齐 | `pip list | grep -iE "isaacgym|torch|wheel_legged"` |
| 4 | 补丁打了 | `grep -c "dtype=float" ~/isaacgym/python/isaacgym/torch_utils.py` → 1 |
| 5 | 环境变量固化 | `echo $LD_LIBRARY_PATH` → 含 envs/leg/lib |
| 6 | GPU 通 | 冒烟测试出 `Physics Device: cuda:0` |



