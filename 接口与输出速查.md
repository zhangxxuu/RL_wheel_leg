# 接口与输出速查（读懂这套代码用）

> 目标：**跑通链路 → 熟悉接口函数 → 熟悉输出函数**。代码里对应位置都加了中文注释块（搜 `【接口` / `【输出` 就能全部跳到）。
> 原理：所有数据都在"接口函数"之间流动；所有结果都从"输出函数"出去。搞清这两类，整条链路就通了。

---

## 一、谁调谁（三层 + 一个调度器）

```
train.py / play.py                     ← 入口脚本
   │
   ├─ TaskRegistry.make_env()          ← 工厂：任务名 → 环境实例
   ├─ TaskRegistry.make_alg_runner()   ← 工厂：环境 + 配置 → 训练器（含日志目录、续训）
   │
   └─ OnPolicyRunner.learn()           ← ★调度器（主循环）
         ├─ PPO.act()                  ← 策略采样动作
         ├─ LeggedRobot.step()         ← ★环境推进一步（仿真 + 奖励 + 复位 + 观测）
         ├─ PPO.process_env_step()     ← 存进 rollout 缓冲
         ├─ PPO.compute_returns()      ← GAE 算优势
         ├─ PPO.update()               ← ★PPO 更新（含 encoder 辅助监督）
         └─ OnPolicyRunner.log()       ← ★输出：终端 + tensorboard
               每 100 轮 → runner.save()  ← ★输出：model_XXXX.pt
```

网络内部（`ActorCriticSequence`）：

```
obs_history(N,125) ─► encoder[128,64] ─► latent(N,3) ─┐
                                                      ├─► actor[128,64,32] ─► actions(N,6)
obs(N,25) ────────────────────────────────────────────┘
privileged_obs(N,141) [+latent] ─► critic[256,128,64] ─► value(N,1)
```

---

## 二、一次迭代的数据流（形状）

| # | 函数（文件:行） | 输入 | 输出 |
|---|---|---|---|
| 1 | `PPO.act` `ppo.py:101` | obs(N,25), obs_history(N,125), critic_obs(N,141) | actions(N,6) + 写入 transition |
| 2 | `LeggedRobot.step` `legged_robot.py:87` | actions(N,6) | obs, priv_obs, rewards(N,), dones(N,), extras, obs_history |
| 3 | `PPO.process_env_step` `ppo.py:123` | rewards, dones, infos | 写入 storage（超时做 bootstrap） |
| 4 | `PPO.compute_returns` `ppo.py:140` | 最后一个 critic_obs | 优势 advantages、回报 returns |
| 5 | `PPO.update` `ppo.py:144` | rollout 缓冲 | 4 个 loss 标量 + 更新网络权重 |
| 6 | `OnPolicyRunner.log` `on_policy_runner.py:183` | locals() | 终端输出 + tensorboard |
| 7 | `OnPolicyRunner.save` `on_policy_runner.py:274` | — | `model_{it}.pt` |

关键常数：`sim.dt=0.005` × `decimation=2` → **env.dt=0.01（控制 100 Hz）**；`num_steps_per_env=48`；`max_episode_length≈2000 步（20 秒）`；`save_interval=100`。

---

## 三、接口函数清单（按重要性）

### 环境侧（env ↔ 算法）
| 函数 | 作用 | 备注 |
|---|---|---|
| `LeggedRobot.step` | RL 主循环唯一推进入口 | 内部 decimation 次物理子步 |
| `LeggedRobot.compute_observations` | 生成 obs/privileged_obs + 维护 5 帧历史 | 改维度必须同步 export 脚本 |
| `LeggedRobot._compute_torques` | 动作 → 力矩（**实车同款控制律**） | 列索引写死，换机器人必改 |
| `LeggedRobot.check_termination` | 写 `reset_buf`（谁该复位） | 摔倒需持续 1 秒 |
| `LeggedRobot.reset_idx` | 部分复位 + 课程推进 + 写 extras | |
| `BaseTask.reset` / `get_observations` | env 对外最基础接口 | |

### 算法侧（算法 ↔ 策略）
| 函数 | 作用 |
|---|---|
| `PPO.act` | 训练时按分布采样 + 记录 log_prob/value |
| `PPO.process_env_step` | 存经验；**超时 bootstrap** 是重点 |
| `PPO.update` | surrogate + value + entropy；自适应 lr；**encoder 第二优化器** |

### 策略侧（训练 ↔ 部署）
| 函数 | 作用 |
|---|---|
| `ActorCriticSequence.act` | 训练：采样动作（含探索） |
| `ActorCriticSequence.act_inference` | **部署/播放：确定性输出**（ONNX 必须与它一致） |
| `ActorCriticSequence.encode` | 只跑 encoder：历史 → latent |
| `ActorCriticSequence.evaluate` | critic 估价 V(s) |

### 工厂/工具侧
| 函数 | 作用 |
|---|---|
| `TaskRegistry.make_env` | 任务名 → env |
| `TaskRegistry.make_alg_runner` | 建日志目录 + 续训（`--resume`） |
| `helpers.get_load_path` | `--load_run/--checkpoint` → 具体 `.pt` 路径（-1=自动最新） |
| `helpers.update_cfg_from_args` | 命令行覆盖配置 |
| `helpers.get_args` | 所有命令行参数的声明处 |

---

## 四、输出函数清单（结果从哪出去）

| 输出到哪 | 谁写的 | 内容 |
|---|---|---|
| **终端** | `OnPolicyRunner.log` | 迭代号、速度(steps/s)、value/surrogate loss、Mean reward、Mean length、ETA |
| **tensorboard** | `OnPolicyRunner.log` | 见下表 |
| **checkpoint** | `OnPolicyRunner.save` | `model_{it}.pt` = 网络权重 + 优化器状态 + 迭代数 + infos |
| **配置快照** | `TaskRegistry.save_cfgs` | run 目录里的 `*_config.py`/`legged_robot.py`/`terrain.py` |
| **训练日志文件** | 你自己 `> train.log` | 终端输出的全文（排查用） |
| **ONNX** | `export_onnx/export_onnx.py` | 部署文件（154KB） |

### tensorboard 指标含义
| 前缀 | 指标 | 怎么看 |
|---|---|---|
| `Train/` | `mean_reward`、`mean_episode_length` | 最近 100 回合均值；长度满格(≈2000)=不摔 |
| `Episode/` | `rew_<每个奖励项>`、`terrain_level`、`a_<地形>_max_command_x` | **逐项奖励**——判断"它为什么学成这样"就看这里；`a_*` 是各地形当前的速度课程上限 |
| `Loss/` | `value_function`、`surrogate`、`encoder`、`learning_rate` | `encoder` 是 latent 的监督损失（估线速度+去噪） |
| `Policy/` | `mean_noise_std`、`mean_kl` | 噪声 std 持续增大=探索过度；KL 长期偏高=lr 太大 |
| `Perf/` | `total_fps`、`collection time`、`learning_time` | 采样 vs 学习耗时，调并行度的依据 |

---

## 五、部署接口（训练 ↔ 实车必须一致）

| 项 | 值（本任务） |
|---|---|
| 输入 1 | `obs` (1, 25) —— 角速度3 + 姿态3 + 指令3 + 腿关节4 + 关节速度6 + 上一步动作6 |
| 输入 2 | `obs_history` (1, 125) = 最近 5 帧 obs 顺序拼接 |
| 输出 | `actions` (1, 6)（可选再输出 `latent`(1,3) 便于调试） |
| 控制频率 | **100 Hz**（`1/(sim.dt × decimation)`） |
| 动作含义 | 腿=位置目标（×0.5 后加 default_pos）；轮=速度目标（×10.0） |
| 对应函数 | `ActorCriticSequence.act_inference` |

⚠️ 任何一项对不上（帧数、维度、顺序、频率），实车就会抖/摔。改动后务必同步 `export_onnx*.py` 里的常量。

---

## 六、用输出反推问题（调试套路）

| 症状 | 先看哪个输出 | 说明 |
|---|---|---|
| Mean reward 不涨 | `Episode/rew_*` 逐项 | 找出是哪一项在恶化（多半是 tracking 或 orientation） |
| 学会"趴着爬" | `Episode/rew_base_height`、`Mean length` | 奖励设计问题：趴着也能满足高度/跟踪，需调权重（见第 10 步记录卡写法） |
| 训练发散/NaN | `Loss/learning_rate`、`Policy/mean_kl` | 自适应 lr 跟不上 → 手动降 lr |
| 腿部乱抖 | `Episode/rew_action_rate`、`rew_dof_acc` | 加大平滑惩罚 |
| encoder 学不动 | `Loss/encoder` | 检查 `critic_obs[:, :3]` 是否真是机身线速度 |
| 速度提不上去 | `Episode/a_*_max_command_x` | 速度课程没推进 → 说明跟踪奖励没达标 |

---

## 七、代码里的两处"坑"（读代码时别被误导）

1. `legged_robot.py` 里 `_process_dof_props` **定义了两次**（约 486 行和 527 行）——后者覆盖前者，改的时候要改**后面那个**。
2. `runner.save()` **没有保存 encoder 的 extra_optimizer 状态**，所以续训时 encoder 的优化器动量是从零开始的（能跑，但不完全等价于连续训练）。
