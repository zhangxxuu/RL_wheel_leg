# 代码导航与调参手册

> **怎么用这本手册**
> 1. 先读 **第一部分 链路全景** —— 建立地图：一次训练从命令行到模型落盘，每一步在哪个函数里
> 2. 想动什么 → 查 **第二部分「我要做什么 → 打开哪里」**
> 3. 出了问题 → 查 **第四部分「调参处方」**（症状 → 改什么 → 看哪个输出验证）
> 4. 核对当前值 → 查 **第三部分「参数总表」**（配置文件的**行号有效**；代码函数一律用名字定位，因为加注释会漂移）
>
> 定位工具：`bash tools/where.sh 关键词`（自动带上函数上方的【接口】/【输出】注释）

```
本工程 = 三层 + 一个调度器
  envs/    仿真环境（机器人、奖励、课程、域随机化）
  rsl_rl/  算法（PPO、策略网络、经验缓冲、训练调度）
  scripts/ 入口（train / play）
  utils/   工厂与工具（任务注册、配置解析、日志、地形）
```

---

# 一、链路全景：一次训练从命令行到落盘

| # | 发生什么 | 位置（文件 :: 函数） | 想改这里就改 |
|---|---|---|---|
| 1 | 解析命令行参数（task/num_envs/resume…） | `utils/helpers.py :: get_args` | 加新命令行参数 |
| 2 | 从注册表取配置 + 命令行覆盖 | `task_registry.py :: make_env` → `helpers.py :: update_cfg_from_args` | 配置项与命令行对应关系 |
| 3 | 构造环境 | `legged_robot.py :: __init__` → `_parse_cfg` | 常用项的预处理 |
| 3.1 | 创建 Isaac Gym sim / viewer | `base_task.py :: __init__` + `legged_robot.py :: create_sim` | 物理引擎、CPU/GPU、headless |
| 3.2 | 加载 URDF、建 N 个并行环境、域随机化 | `legged_robot.py :: _create_envs` → `_process_rigid_shape_props` / `_process_dof_props` / `_process_rigid_body_props` | 机器人资产、摩擦/质量/Kp 随机化 |
| 3.3 | 抓取仿真张量（零拷贝） | `legged_robot.py :: _init_buffers` | 读哪些仿真数据 |
| 3.4 | 奖励“接线” | `legged_robot.py :: _prepare_reward_function` | 每个奖励项的启用/禁用（权重 0 即禁用） |
| 4 | 建训练器 + 日志目录（+ 续训加载） | `task_registry.py :: make_alg_runner` → `runner.load` | 日志命名、resume 逻辑 |
| 5 | 写配置快照进 run 目录 | `task_registry.py :: save_cfgs` | 快照包含哪些文件 |
| 6 | **主循环**（每 iteration） | `on_policy_runner.py :: learn` | 训练循环本身 |
| 6.1 | 采样 48 步：策略出动作 → 环境推进 → 存经验 | `ppo.py :: act` → `legged_robot.py :: step` → `ppo.py :: process_env_step` | 这三步是“接口主链” |
| 6.2 | GAE 算优势/回报 | `ppo.py :: compute_returns` → `rollout_storage.py :: compute_returns` | 折扣 γ、λ |
| 6.3 | PPO 更新（含 encoder 辅助监督） | `ppo.py :: update` | 损失项、lr 调度、epoch 数 |
| 6.4 | 输出指标 | `on_policy_runner.py :: log` | 终端/tensorboard 显示什么 |
| 6.5 | 每 100 轮存盘 | `on_policy_runner.py :: save` | checkpoint 内容与频率 |
| 7 | 导出 ONNX → 拷回本地 → mujoco 验证 | `export_onnx/export_onnx.py :: export_onnx` | 部署接口 |

**环境推进的细节**（`legged_robot.py :: step` 内部）：
```
actions(N,6) → 裁剪 → 动作延迟 FIFO → 重复 decimation(=2) 次:
    _compute_torques()  (腿:位置PD / 轮:速度PD)  →  set_dof_actuation_force → simulate
→ post_physics_step():
    refresh 张量 → 派生量(线/角速度、姿态、dof_acc) → 虚拟腿 L0/theta0
    → _post_physics_step_callback(每5秒重采样指令)
    → check_termination() → compute_reward() → reset_idx(该复位的) → compute_observations()
```

---

# 二、"我要做什么" → 打开哪里

## A. 训练与算法
| 我要… | 打开 | 关键点 |
|---|---|---|
| 改训练轮数/日志目录名 | `legged_robot_config.py` `class runner`（`max_iterations` / `experiment_name` / `run_name` / `save_interval`） | 任务级在 `wheel_legged_config.py::WheelLeggedCfgPPO.runner`（现在 `max_iterations=50000`） |
| 改学习率/训练稳定性 | 同文件 `class algorithm`（`learning_rate` / `desired_kl` / `clip_param` / `entropy_coef` / `num_learning_epochs` / `num_mini_batches`） | 学习率是**最常调**的一项 |
| 改网络大小/结构 | 同文件 `class policy`（`actor_hidden_dims` / `critic_hidden_dims` / `encoder_hidden_dims` / `latent_dim` / `init_noise_std`） | `latent_dim` 至少 3（估线速度） |
| 改一次采样多少步 | `class runner` 的 `num_steps_per_env`（=48） | 与 `num_mini_batches` 共同决定 batch |
| 改 PPO 更新细节（损失/双优化器） | `rsl_rl/algorithms/ppo.py :: update` | encoder 的辅助损失也在这 |
| 改 GAE 细节 | `rsl_rl/storage/rollout_storage.py :: compute_returns` / `mini_batch_generator` | |
| 改训练主循环（加指标、改保存策略） | `rsl_rl/runners/on_policy_runner.py :: learn` | |

## B. 奖励与课程（调参主战场）
| 我要… | 打开 | 关键点 |
|---|---|---|
| 改奖励权重 | `legged_robot_config.py` `class rewards.scales`（第 184 行起） | 权重为 0 = 该项关闭 |
| 加一个新奖励 | ① `scales` 里加 `my_reward = 1.0` ② `legged_robot.py` 写 `def _reward_my_reward(self)` 返回 (N,) | 名字必须严格对应；权重会被自动 ×dt |
| 改奖励的形状（高斯宽度） | `class rewards` 的 `tracking_sigma`（0.25）、`base_height` 的高斯宽度写在函数里 | σ 越小 → 越严格 |
| 改目标站立高度 | `base_height_target`（0.18）+ `class commands.ranges.height`（0.10~0.20） | ⚠️ 高度是“离地高度”，不是世界 z |
| 改指令范围 | `class commands.ranges`（`lin_vel_x` / `ang_vel_yaw` / `height`） | 与课程上限配合 |
| 改课程（地形升级/降级、速度放宽） | `legged_robot.py :: _update_terrain_curriculum` / `update_command_curriculum` + `class commands`（`curriculum`、`basic_/advanced_max_curriculum`、`curriculum_threshold`） | 失败会收紧速度范围 |
| 关掉课程（做对照实验） | `class terrain.curriculum=False` + `class commands.curriculum=False` | 关掉后速度课程走另一分支 |
| 开随机地形 | `class terrain`：`mesh_type="trimesh"` + `terrain_proportions`（想用梅花桩/沟/坑要配 **8 个数**） | 生成逻辑在 `utils/terrain.py` |
| 改地形难度/规模 | `num_rows` / `num_cols` / `max_init_terrain_level` / `terrain_length` | |

## C. 机器人与控制
| 我要… | 打开 | 关键点 |
|---|---|---|
| 换机器人 URDF | `wheel_legged_config.py` `class asset.file`（第 70 行） | 详见文末“换 URDF 前的准备” |
| 改 PD 刚度/阻尼 | `wheel_legged_config.py` `class control`（`stiffness` / `damping`，**按关节名子串匹配**） | 现在 `f0/f1=20`、`wheel=0` |
| 改动作缩放（力度） | `wheel_legged_config.py` `pos_action_scale`（0.5）/ `vel_action_scale`（10.0） | 决定动作→目标的换算 |
| 改控制律本身（位置/速度/力矩混合） | `legged_robot.py :: _compute_torques` | ⚠️ 里面轮子列索引 `[:, [2,5]]` 是**写死的** |
| 改控制频率 | `class control.decimation`（=2）+ `class sim.dt`（=0.005） | env.dt = decimation×sim.dt = 0.01 → 100 Hz |
| 改初始姿态/出生高度 | `wheel_legged_config.py` `class init_state`（`pos` / `default_joint_angles`） | 关节名必须与 URDF 一致 |
| 改摔倒判定 | `legged_robot.py :: check_termination` + `class env.fail_to_terminal_time_s`（=1s）+ `class asset.terminate_after_contacts_on` | 现在是空列表 → 无“撞地终止” |
| 改回合长度 | `class env.episode_length_s`（=20） | max_episode_length = 20/0.01 = 2000 步 |
| 开域随机化（sim2real） | `class domain_rand`（第 158 行起，共 11 项） | 动作延迟/推力默认**关** |
| 改观测噪声 | `class noise`（第 230 行起）`add_noise` / `noise_level` / `noise_scales` | 按观测项分别设 |
| 改观测内容 | `legged_robot.py :: compute_proprioception_observations`（actor 用 25 维）/ `compute_observations`（历史 + 特权观测） | ⚠️ 连带改 `env.num_observations`、`policy.num_encoder_obs`、`export_onnx*.py` |
| 改特权观测（critic 用） | `compute_observations` 里 `privileged_obs_buf` 的拼接 + `env.num_privileged_obs` | 改完要保证与 critic 输入一致 |

## D. 输出与部署
| 我要… | 打开 | 关键点 |
|---|---|---|
| 改终端/tensorboard 输出 | `on_policy_runner.py :: log` | 增加指标就加 `writer.add_scalar` |
| 让 env 上报新指标 | `legged_robot.py :: reset_idx` 里写 `self.extras["episode"]["xxx"]` | 会自动进 tensorboard 的 `Episode/` |
| 改 checkpoint 内容/频率 | `on_policy_runner.py :: save` + `class runner.save_interval` | ⚠️ 现在没存 encoder 的 extra_optimizer 状态 |
| 加载模型/续训 | `helpers.py :: get_load_path` + `on_policy_runner.py :: load` | `-1` = 自动最新 |
| 导出 ONNX | `export_onnx/export_onnx.py :: export_onnx`；本机可用 `bash ~/isaacgym/export_local.sh` | 常量：`NUM_OBS/OBS_HISTORY_LEN/LATENT_DIM/NUM_ACTIONS` |
| 键盘操控仿真 | `scripts/play.py :: apply_manual_commands` / `play` | 直接改 `env.commands` |
| 看某个策略的实际输出 | `actor_critic_sequence.py :: act_inference` | 返回 (actions, latent) |

---

# 三、参数总表（配置文件行号有效；⚠️ 改完记得看第四部分验证）

## A. `class env`（第 35 行起）
| 参数 | 当前 | 作用 | 怎么调 |
|---|---|---|---|
| `num_envs` | 8192 | 并行环境数 | 24G 显存建议 4096；显存富裕再往上 |
| `num_observations` | 25 | actor 观测维度 | 改观测内容才改，连带 3 处 |
| `obs_history_length` | 5 | 历史帧数 | 改 10 必须换 `export_onnx_his10.py` |
| `num_actions` | 6 | 动作维度 | = 驱动机器人的关节数 |
| `episode_length_s` | 20 | 回合秒数 | 太短学不会长程，太长拖慢评估 |
| `fail_to_terminal_time_s` | 1 | 摔倒判定需持续秒数 | 调小=复位更快、更严 |

## B. `class control`（第 115 行；任务级覆盖在 `wheel_legged_config.py` 第 58 行）
| 参数 | 当前（本任务） | 作用 | 怎么调 |
|---|---|---|---|
| `stiffness` | f0=20, f1=20, wheel=0 | 位置环 Kp | 站不直/软 → 加大（25~40）；抖动 → 减小 |
| `damping` | f0=1.0, f1=1.0, wheel=0.2 | 速度环 Kd | 振荡 → 加大；轮子打滑 → wheel 加大 |
| `pos_action_scale` | 0.5 | 动作→位置目标 | 动作幅度不够 → 加大 |
| `vel_action_scale` | 10.0 | 动作→速度目标 | 轮速上不去 → 加大 |
| `decimation` | 2 | 每个策略步的物理子步数 | 改控制频率（sim2real 必须与实车一致） |

## C. `class rewards.scales`（第 184 行起）★ 调参核心
| 奖励项 | 当前权重 | 含义 | 调大的效果 | 副作用 |
|---|---|---|---|---|
| `tracking_lin_vel` | 1.0 | 速度跟踪 `exp(-err²/0.25)` | 跑得更积极 | 可能牺牲稳定 |
| `tracking_lin_vel_enhance` | 1.0 | `exp(-err²/2.5)-1`（恒负，更狠） | 更强逼迫跟踪 | 早期难学 |
| `tracking_ang_vel` | 1.0 | 转向跟踪 | 转向更准 | |
| `tracking_ang_vel_enhance` | 1.0 | 同上更狠版本 | | |
| `base_height` | 1.0 | `exp(-(h-h_cmd)²/0.001)` **极窄高斯** | **逼它站到目标高度** | 太大会“僵直”不敢动 |
| `orientation` | **-100** | 机身倾斜惩罚 | （已是最大负项）再大 → 更怕歪 | 过大导致不敢倾斜、动作保守 |
| `nominal_state` | -1.0 | 左右虚拟腿角不一致惩罚 | 更对称 | 与“单腿支撑”类动作冲突 |
| `lin_vel_z` | -1.0 | 机身上下窜 | 更平顺 | |
| `ang_vel_xy` | -0.2 | 机身翻滚角速度 | 更稳 | 转向灵活性下降 |
| `dof_vel` | -5e-5 | 关节速度 | 更慢更稳 | 学得慢 |
| `dof_acc` | -2.5e-7 | 关节加速度 | 更平滑 | 同上 |
| `torques` | -0.0001 | 力矩（省力） | 省电、发热小 | 力不够 → 站不起来 |
| `action_rate` | -0.01 | 动作变化率 | 抖动减少 | 响应变慢 |
| `action_smooth` | -0.01 | 动作二阶平滑 | 同上 | 同上 |
| `collision` | -1.0 | 非足端碰撞 | 少撞 | |
| `dof_pos_limits` | -1.0 | 关节超软限位 | 少打限位 | |
| `theta0_equ_0` | 注释掉 (0.4) | 虚拟腿角归零（竖直） | 想强制“腿竖直站立”可打开 | 与蹲起动作冲突 |

> 另有 3 个奖励函数**写了但没配权重**（等同关闭）：`stumble`（踢台阶）、`stand_still`（零指令时别乱动）、`feet_contact_forces`（限接触力）。要用就在 `scales` 里加权重。

## D. `class rewards` 其它 / `class commands` / `class terrain`
| 参数 | 当前 | 作用 |
|---|---|---|
| `tracking_sigma` | 0.25 | 跟踪奖励的高斯宽度（越小越严格） |
| `base_height_target` | 0.18 | 默认目标离地高度 |
| `soft_dof_pos_limit` / `soft_torque_limit` | 0.9 / 1.0 | 软限位比例（用于惩罚范围） |
| `max_contact_force` | 100 | 接触力惩罚阈值 |
| `commands.ranges.lin_vel_x` | [-2, 2] | 前向速度指令范围 |
| `commands.ranges.ang_vel_yaw` | [-2, 2] | 转向指令范围 |
| `commands.ranges.height` | [0.10, 0.20] | 目标高度指令范围 |
| `commands.resampling_time` | 5.0 | 指令保持/重采样周期 |
| `commands.curriculum` 及 `basic/advanced_max_curriculum` | True / 2.5 / 1.5 | 速度课程开关与上限 |
| `terrain.mesh_type` | "plane" | 平地；改 `trimesh` 开随机地形 |
| `terrain.terrain_proportions` | [0.2,0.2,0.2,0.1,0.2,0.1] | 各类型占比（**8 个数**才能启用梅花桩/沟/坑） |
| `terrain.num_rows / num_cols` | 10 / 20 | 地形等级数 / 类型数 |
| `terrain.curriculum` | True | 地形课程开关 |

## E. `class domain_rand`（第 158 行起，sim2real 的关键）
| 项 | 当前 | 建议 |
|---|---|---|
| `randomize_friction` 0.6~1.4 | 开 | 保持开（真实地面差异大） |
| `randomize_restitution` 0.6~1.0 | 开 | |
| `randomize_base_mass` −1~+2 kg | 开 | 按实车挂载（云台/弹仓）范围调 |
| `randomize_inertia` ±10% / `randomize_base_com` ±2cm | 开 | |
| `randomize_Kp/Kd` ±5% | 开 | 电机差异 |
| `randomize_motor_torque` ±5% | 开 | |
| `randomize_default_dof_pos` ±0.03 | 开 | |
| `randomize_action_delay` 0~10ms | **关** | **上实车前建议打开**（通信延迟） |
| `push_robots` 2 m/s | **关** | 抗扰训练可打开 |

## F. `class noise` / `class normalization` / `class policy` / `class algorithm` / `class runner`
| 参数 | 当前 | 作用 / 怎么调 |
|---|---|---|
| `noise.add_noise` / `noise_level` | True / 1.0 | 观测噪声总开关与倍率（sim2real 必开） |
| `noise_scales.*` | dof_pos 0.02, dof_vel 1.5, lin_vel 0.1, ang_vel 0.2 | 按传感器精度调 |
| `normalization.clip_observations/actions` | 100 / 100 | 裁剪上限 |
| `normalization.obs_scales.*` | lin_vel 2, ang_vel 0.25, dof_pos 1, dof_vel 0.05… | 归一化尺度，改观测时同步 |
| `policy.init_noise_std` | 0.5 | 初始探索强度；学不动可加大 |
| `policy.actor/encoder_hidden_dims` | [128,64,32] / [128,64] | 容量；太小欠拟合，太大易过拟合 |
| `policy.latent_dim` | 3 | ≥3（前 3 维估线速度） |
| `algorithm.learning_rate` | **5.0e-4** | 发散就降（2e-4~1e-4），学得慢就升 |
| `algorithm.extra_learning_rate` | 1e-3 | encoder 辅助任务的学习率 |
| `algorithm.entropy_coef` | 0.01 | 探索；策略过早收敛可加大 |
| `algorithm.num_learning_epochs` / `num_mini_batches` | 5 / 4 | 更新次数与批大小 |
| `algorithm.clip_param` / `desired_kl` | 0.2 / 0.005 | PPO 裁剪；KL 目标（自适应 lr 依据） |
| `algorithm.gamma` / `lam` | 0.99 / 0.95 | 折扣与 GAE λ |
| `runner.num_steps_per_env` | 48 | 每轮采样步数 |
| `runner.max_iterations` | 5000（任务级 50000） | 训练总轮数 |
| `runner.save_interval` | 100 | 存 checkpoint 间隔 |
| `runner.experiment_name` | "test"（任务级 "wheel_legged"） | 日志目录名 |

---

# 四、调参处方（症状 → 改什么 → 看什么验证）

> **铁律**：一次只改 1~2 个变量；改前 `git commit` 记基线；每次实验按第 10 步的“记录卡”写清楚。

| 症状 | 处方（按优先级） | 验证指标（tensorboard） |
|---|---|---|
| **学会“趴着爬”不肯站** | ① `base_height` 权重 1.0→2.0~3.0 ② 打开 `theta0_equ_0`（0.3~0.5）③ `base_height_target` 略升 | `Episode/rew_base_height` 转正、`Train/mean_episode_length` 满格 |
| 站起来了但一直抖/不稳 | ① `action_rate` / `action_smooth` 加大（−0.01→−0.03）② `dof_acc` 略加大 ③ Kd（damping）加大 | `Episode/rew_action_rate`、`rew_dof_acc` 绝对值下降 |
| 腿软、撑不住（高度到不了目标） | ① `stiffness.f0/f1` 20→30 ② `torques` 惩罚减小 | `Episode/rew_base_height`、`rew_torques` |
| 轮子打滑/原地打转 | ① `damping.wheel` 0.2→0.3~0.5 ② `vel_action_scale` 略降 | `Episode/rew_tracking_lin_vel` |
| 速度跟踪差、跑不快 | ① 检查 `Episode/a_*_max_command_x` 是否已放宽（课程没推进说明没达标）② `tracking_sigma` 0.25→0.15（更严格）③ `base_height` 权重过大压制了运动 → 略降 | `a_*_max_command_x`、`rew_tracking_lin_vel` |
| 训练发散 / NaN | ① `learning_rate` 5e-4→2e-4 ② `desired_kl` 0.005→0.003 ③ `entropy_coef` 加大一点 | `Loss/learning_rate`（自适应是否在降）、`Policy/mean_kl` |
| 学得太慢 | ① `num_envs` 加大 ② `learning_rate` 略升 ③ 打开 `base_height` 等“引导性”奖励 | `Perf/total_fps`、`Train/mean_reward` 斜率 |
| 平地会走、上台阶就摔（泛化差） | ① `mesh_type="trimesh"` + 配 8 档 `terrain_proportions` ② 保持地形课程开 ③ 打开 `randomize_action_delay`、`push_robots` | `Episode/terrain_level`（能否升到高级地形） |
| 仿真好、实车抖/摔（sim2real） | ① 打开 `randomize_action_delay` ② 噪声确认开（`noise.add_noise`）③ Kp/Kd/质量随机化确认开 ④ 控制频率与实车对齐（`decimation`×`sim.dt`） | 仿真里“在随机化下”的 `Train/mean_reward` |
| encoder 学不动 | 检查 `Loss/encoder`；确认 `critic_obs[:, :3]` 是机身线速度（观测顺序变了会错位） | `Loss/encoder` 是否下降 |
| 探索过度（动作噪声不收敛） | `entropy_coef` 调小（0.01→0.005） | `Policy/mean_noise_std` 应缓慢下降 |

---

# 五、改动的“连带清单”（改了 A 必须同步 B）

| 你改了 | 必须同步 |
|---|---|
| `num_observations` / `obs_history_length` | `policy.num_encoder_obs`、`export_onnx*.py` 的 `NUM_OBS/OBS_HISTORY_LEN`（10 帧要用 `export_onnx_his10.py`） |
| `num_actions` / DOF 顺序 | `_compute_torques` 里写死的列索引、`init_state.default_joint_angles`、`commands` 维度、`play.py` 键控 |
| `decimation` / `sim.dt` | 所有用 `self.dt` 的地方（奖励 ×dt、课程阈值、动作延迟换算、回合长度） |
| 奖励项改名 | `_reward_<同名>` 必须存在，否则 `getattr` 直接报错 |
| `latent_dim` | `ppo.py` 的 `obs_denoise_loss` 分支、ONNX 导出常量 |
| `mesh_type` / 地形比例 | 地形课程与 `measured_heights`（特权观测里的身高扫描） |
| 新加机器人 task | `envs/<task>/<task>_config.py` + `envs/__init__.py` 里 `task_registry.register()`（复旦 task 不用动） |

---

# 六、30 分钟读代码顺序（跟着链路走）

1. `scripts/train.py`（55 行，看入口）
2. `utils/task_registry.py :: make_env` / `make_alg_runner`（工厂与日志/续训）
3. `envs/base/legged_robot.py :: step` → `_compute_torques` → `post_physics_step` → `compute_observations`
4. `envs/base/legged_robot.py :: _prepare_reward_function` + `compute_reward`（奖励怎么接起来）
5. `rsl_rl/runners/on_policy_runner.py :: learn`（主循环）→ `log`（输出）
6. `rsl_rl/algorithms/ppo.py :: act` / `process_env_step` / `update`（算法接口）
7. `rsl_rl/modules/actor_critic_sequence.py :: act_inference`（部署接口）
8. 需要时再看：`utils/terrain.py`、`rollout_storage.py`、`utils/logger.py`

配合阅读：`接口与输出速查.md`（形状与输出清单）、`bash tools/where.sh 关键词`（定位）。

---

# 七、换 URDF 之前，先把这 6 件事确认了（学长说的“先跑通链路”）

1. **能独立跑通**：训练 → 停止 → 续训（`--resume`）→ 导出 ONNX → 本地 mujoco 播放，全流程不查文档也能走
2. **接口吃透**：`env.step` 的六元组、`act_inference(obs, obs_history) → (actions, latent)`、ONNX 的输入输出名字与形状
3. **输出吃透**：终端每行在说什么；tensorboard 五个前缀（`Train/ Episode/ Loss/ Policy/ Perf/`）分别代表什么；`Episode/rew_*` 怎么用来判断“它为什么学成这样”
4. **调参能闭环**：改一个权重 → 跑 500 轮 → 从 `Episode/rew_*` 看出变化 → 写记录卡
5. **备份与恢复能自救**：`backup_sync.sh` 在跑、`export_local.sh` 能出 ONNX、实例重建后能 `ssh-copy-id` + `sync_to_server.sh` 恢复
6. **再动 URDF**：那时你已经知道要改哪 9 处（见《接口与输出速查》与本文第二部分 C 组）
